{
  "name": "notebook-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive page-based development environment: executables with a shared namespace, observable data components, and lit-html UI components.",
  "workspaces": [
    "frontend",
    "infra"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "typecheck": "npm run typecheck --workspace frontend && npm run typecheck --workspace infra",
    "build:frontend": "npm run build --workspace frontend",
    "synth": "npm run synth --workspace infra",
    "deploy": "npm run build:frontend && npm run deploy --workspace infra",
    "test": "npm run build:frontend && node tests/smoke.js"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1"
  },
  "engines": {
    "node": ">=20"
  }
}
